FREEMARKER QUICK REFERENCE
--------------------------

object?has_content

object?? (ignores null)

object!
object!default
(object.sub)!

<#-- Comment -->

<#list sequence as item>
    ${item_index} <#-- Counter (zero based) --> 
    <#if item_has_next>More to come...</#if>
</#list>

boolean?string
boolean?string("yes", "no")

lastUpdated?datetime?string.short
short/medium/long/full
lastUpdated?string("yyyy-MM-dd HH:mm:ss zzzz")