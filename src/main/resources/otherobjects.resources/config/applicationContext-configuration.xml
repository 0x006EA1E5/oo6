<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans-2.5.xsd 
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context-2.5.xsd">

	<context:annotation-config />

	<bean name="singletonBeanLocator" class="org.otherobjects.cms.SingletonBeanLocator" />

	<bean id="annotatedClassesScanner" class="org.otherobjects.cms.discovery.AnnotatedClassesScannerImpl" />

	<!-- For properties -->
	<bean id="otherObjectsConfigurator" class="org.otherobjects.cms.config.OtherObjectsConfigurator">
		<property name="ignoreResourceNotFound" value="true" />
		<property name="locations">
			<list>
				<value>classpath:otherobjects.resources/config/otherobjects.properties</value>
				<value>classpath:site.resources/config/site.properties</value>
				<value>classpath:site.resources/config/pom.properties</value>
			</list>
		</property>
	</bean>
	
	<bean id="ooResourceLoader" class="org.otherobjects.cms.io.OoResourceLoader">
	</bean>

	<!-- JNDI DataSource for J2EE environments -->
	<!--<jee:jndi-lookup id="dataSource" jndi-name="java:comp/env/jdbc/appfuse"/>-->

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
		<property name="maxActive" value="100" />
		<property name="maxIdle" value="30" />
		<property name="maxWait" value="1000" />
		<property name="defaultAutoCommit" value="true" />
		<property name="removeAbandoned" value="true" />
		<property name="removeAbandonedTimeout" value="60" />
	</bean>

	<!-- Groovy telnet shell -->
	<bean id="groovyShellService" class="org.otherobjects.cms.scripting.groovy.GroovyShellService" init-method="initialize">
		<property name="socket" value="6789" />
		<!-- TODO Should not launch at startup. Could be lauched via Debug page instead. Also 
		should have port as a setting and if null should we should disable this service. -->
		<property name="launchAtStart" value="true" />
	</bean>
</beans>
