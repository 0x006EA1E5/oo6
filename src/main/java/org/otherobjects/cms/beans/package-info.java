/**
 * Classes in this package deal with 'beanifying' objects that might not be proper beans to start with 
 * in order to use them in places where one needs proper beans (binding, validation etc.)
 */
package org.otherobjects.cms.beans;